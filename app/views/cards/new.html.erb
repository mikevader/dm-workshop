<% provide(:title, 'New card') %>

<h1>Card</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@card) do |f| %>
        <%= render 'fields', f: f %>
        <%= f.submit "Create new card", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
