<%
  index = card_counter
  next_index = (index + 1)%@cards.size
  prev_index = (index - 1)%@cards.size
%>

<%= render partial: 'shared/modal', locals: { card: card, index: index, next_index: next_index, prev_index: prev_index } %>
