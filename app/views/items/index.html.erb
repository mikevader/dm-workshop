<% provide(:title, 'All items') %>
<h1>All items</h1>


<%= link_to "New", new_item_path, class: 'btn btn-default', role: 'button' %>

<%= will_paginate %>

<ul class="items">
  <%= render @items %>
</ul>

<%= will_paginate %>
