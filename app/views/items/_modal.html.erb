<%
   index = item_counter
   next_index = (index + 1)%@items.size
   prev_index = (index - 1)%@items.size
%>

<%= render partial: 'shared/modal', locals: { card: item, index: index, next_index: next_index, prev_index: prev_index } %>
