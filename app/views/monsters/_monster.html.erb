<tr>
  <td><button type="button" class="btn btn-primary btn-xs" data-toggle="modal" data-target="#myModal-<%= monster_counter %>">
  o
</button></td>
  <td><%= monster.name %></td>
  <td><%= "#{monster.size} #{monster.monster_type}" %></td>
  <td><%= monster.challenge %></td>
  <% if admin_user? %>
      <td><%= link_to edit_monster_path(monster) do -%>
            <span class="glyphicon glyphicon-pencil" aria-label="edit"></span>
        <% end -%>
      </td>
      <td><%= link_to monster, method: :delete, data: { confirm: "You sure?" } do -%>
            <span class="glyphicon glyphicon-remove" arial-label="delete"></span>
        <% end -%>
      </td>
  <% end %>
</tr>
