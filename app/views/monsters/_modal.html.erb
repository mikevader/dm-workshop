<%
   index = monster_counter
   next_index = (index + 1)%@monsters.size
   prev_index = (index - 1)%@monsters.size
%>

<%= render partial: 'shared/modal', locals: {card: monster, large: true, index: index, next_index: next_index, prev_index: prev_index } %>
